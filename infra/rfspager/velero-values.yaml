---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: velero
  namespace: velero
spec:
  values:
    backupsEnabled: true
    snapshotsEnabled: true
    configuration:
      provider: aws
      backupStorageLocation:
        bucket: rfspager-backups
